//strategy Pattern 
//we can implement the strategy pattern and encapsulate each one them, and make them interchangeable.
// it allows a method to be swapped out at runtime by any other method(strategy) without the client realizing it
